apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: ordercanceled
  namespace: dapr-deliveryapi
spec:
  type: bindings.mqtt
  version: v1
  metadata:
  - name: url
    value: mqtt://localhost:1883
  - name: topic
    value: deliveryapi/partnerservice/ordercanceled
  - name: consumerID
    value: "{uuid}"
scopes:
  - partnerservice